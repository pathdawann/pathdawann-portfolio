---
import Layout from '../layouts/BlogPost.astro';
---

<Layout
    title="Mi Portafolio"
    description="Proyectos destacados de Pathdawann"
    pubDate={new Date('August 08 2025')}
>
    <div class="portfolio-header">
        <h1>Mis Proyectos</h1>
        <p>Una selección de mis trabajos en desarrollo web, estrategia y recursos educativos.</p>
    </div>

    <div class="portfolio-grid">
        
        <article class="card">
            <div class="card-image">
                <img src="/images/proyectoUno.webp" alt="Elemental Race Landing Page" />
            </div>
            <div class="card-content">
                <div class="card-top">
                    <h3>Elemental Race</h3>
                    <span class="badge">Landing Page</span>
                </div>
                <p>Landing page para evento deportivo con banners, sliders, video y llamadas a la acción (CTA) optimizadas para conversión.</p>
                
                <div class="tech-stack">
                    <span>HTML</span>
                    <span>CSS</span>
                    <span>JS</span>
                    <span>Responsive</span>
                </div>
                
                <div class="card-links">
                    <a href="https://elementalrace.netlify.app" target="_blank" class="link-btn primary">Ver Demo</a>
                    <a href="https://github.com/patriciamagodev/elementalRaceProject" target="_blank" class="link-btn outline">Código</a>
                </div>
            </div>
        </article>

        <article class="card">
            <div class="card-image">
                <img src="/images/proyectoDos.webp" alt="Website Blog Setup" />
            </div>
            <div class="card-content">
                <div class="card-top">
                    <h3>Website Blog</h3>
                    <span class="badge">Website / Blog</span>
                </div>
                <p>Blog personal con secciones estructuradas: sobre mí, noticias, posts y formulario de contacto funcional.</p>
                
                <div class="tech-stack">
                    <span>HTML</span>
                    <span>CSS</span>
                    <span>Responsive</span>
                </div>
                
                <div class="card-links">
                    <a href="https://pmdevmaquetacioncss.netlify.app/" target="_blank" class="link-btn primary">Ver Demo</a>
                    <a href="https://github.com/patriciamagodev/practicaMaquetacionCSS" target="_blank" class="link-btn outline">Código</a>
                </div>
            </div>
        </article>

        <article class="card">
            <div class="card-image">
                <img src="/images/proyectoTres.webp" alt="E-Book HTML Cover" />
            </div>
            <div class="card-content">
                <div class="card-top">
                    <h3>E-Book: HTML Básico</h3>
                    <span class="badge highlight">Recurso Educativo</span>
                </div>
                <p>Guía práctica "Cosas que debemos saber sobre HTML" con ejemplos claros para principiantes.</p>
                
                <div class="tech-stack">
                    <span>Canva</span>
                    <span>MDN Docs</span>
                </div>
                
                <div class="card-links">
                    <a href="https://cutt.ly/0rulucjy" target="_blank" class="link-btn primary full-width">Descargar PDF</a>
                </div>
            </div>
        </article>

    </div>
</Layout>

<style>
    /* CABECERA */
    .portfolio-header {
        text-align: center;
        margin-bottom: 4rem;
        padding-top: 2rem;
    }
    
    .portfolio-header h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
        color: var(--text-main);
    }

    .portfolio-header p {
        font-size: 1.2rem;
        color: var(--text-muted);
        max-width: 700px;
        margin: 0 auto;
    }

    /* GRID - CORREGIDO PARA MÓVIL */
    .portfolio-grid {
        display: grid;
        /* CAMBIO CLAVE: 280px en lugar de 320px */
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 2.5rem;
        padding-bottom: 4rem;
        justify-content: center; /* Centra las tarjetas si sobra espacio */
    }

    /* TARJETAS */
    .card {
        background-color: var(--surface);
        border: 1px solid var(--border);
        border-radius: 16px;
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        display: flex;
        flex-direction: column;
        height: 100%;
    }

    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        border-color: var(--accent);
    }

    /* IMAGEN */
    .card-image {
        width: 100%;
        height: 220px;
        overflow: hidden;
        border-bottom: 1px solid var(--border);
    }
    
    .card-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }

    .card:hover .card-image img {
        transform: scale(1.05);
    }

    /* CONTENIDO */
    .card-content {
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        flex-grow: 1;
    }

    .card-top {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1rem;
        gap: 1rem;
    }

    .card-content h3 {
        margin: 0;
        color: var(--text-main);
        font-size: 1.4rem;
        line-height: 1.2;
    }

    .badge {
        font-size: 0.7rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        background-color: var(--background);
        color: var(--text-muted);
        padding: 4px 8px;
        border-radius: 4px;
        font-weight: bold;
        border: 1px solid var(--border);
        white-space: nowrap;
    }
    
    .badge.highlight {
        color: var(--accent);
        border-color: var(--accent);
    }

    .card-content p {
        font-size: 0.95rem;
        color: var(--text-muted);
        margin-bottom: 1.5rem;
        line-height: 1.6;
        flex-grow: 1;
    }

    /* TECNOLOGÍAS (Pills) */
    .tech-stack {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-bottom: 1.5rem;
    }
    
    .tech-stack span {
        font-size: 0.8rem;
        background-color: var(--background);
        color: var(--text-main);
        padding: 0.2rem 0.6rem;
        border-radius: 50px;
        border: 1px solid var(--border);
        font-weight: 500;
    }

    /* BOTONES */
    .card-links {
        display: flex;
        gap: 1rem;
        margin-top: auto;
    }

    .link-btn {
        padding: 0.6rem 1.2rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
        font-size: 0.9rem;
        text-align: center;
        transition: all 0.2s;
        flex: 1;
    }

    .link-btn.primary {
        background-color: var(--accent);
        color: white;
    }
    
    .link-btn.primary:hover {
        background-color: var(--accent-dark);
    }

    .link-btn.outline {
        background-color: transparent;
        border: 1px solid var(--border);
        color: var(--text-main);
    }

    .link-btn.outline:hover {
        border-color: var(--accent);
        color: var(--accent);
    }
    
    .full-width {
        width: 100%;
    }

    /* AJUSTES GLOBALES PARA ANCHO COMPLETO */
    /* Aquí añadimos overflow-x hidden al main para evitar scroll horizontal indeseado */
    :global(main), :global(.prose) {
        width: 100% !important;
        max-width: 1200px !important;
        margin-left: auto !important;
        margin-right: auto !important;
        padding-left: 1.5rem !important;
        padding-right: 1.5rem !important;
        box-sizing: border-box !important;
        overflow-x: hidden; /* Seguridad extra */
    }

    /* LIMPIEZA VISUAL */
    :global(.prose .title), 
    :global(.prose .date),
    :global(main > article > .hero-image + div > .date),
    :global(main > article > div > h1) {
        display: none !important;
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
        .portfolio-header h1 {
            font-size: 2.5rem;
        }
        .card-top {
            flex-direction: column;
            gap: 0.5rem;
        }
    }
</style>